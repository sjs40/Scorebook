# Cloudflare Pages project metadata (experiment branch).
# KV binding POTN_VOTES is configured in the Cloudflare dashboard:
# Pages → scorebook → Settings → Functions → KV namespace bindings.
# Do not add [[kv_namespaces]] ids here — Pages deploy rejects placeholder values.

name = "scorebook"
compatibility_date = "2024-08-01"
pages_build_output_dir = "."
